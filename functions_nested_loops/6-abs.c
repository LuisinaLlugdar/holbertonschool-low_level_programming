#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *
 * @n: variable
 *
 * Return: n if positive or zero, -n otherwise.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
