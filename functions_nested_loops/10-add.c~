#include "main.h"
/**
 * add - ads two integers
 *
 * Return: the result of the addition
 *
 * @a: variable
 *
 * @b: variable
 */
int add(int, int)
{
int a, b, c;
c = a + b;
_putchar(c + '0');
return (c);
}
