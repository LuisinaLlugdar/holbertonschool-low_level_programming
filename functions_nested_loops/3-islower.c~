#include "main.h"

/**
 * _islower - checks for lowercase character.
 *
 * Return: 1 if lowercase, 0 otherwise.
 *
 * @c: variable
 */

int _islower(int c);
int n;
{
if (_islower(c))
{
_putchar(n = '1');
}
else
{
_putchar(n = '0');
}
return (n);
}
